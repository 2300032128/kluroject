# EduAgent AI Frontend Configuration
# Note: ONLY public variables prefixed with NEXT_PUBLIC_ are accessible in client code.
# Sensitive API keys MUST stay on backend server side.

NEXT_PUBLIC_API_URL="http://localhost:8000"
